config MACH
	symbol
	default udoo-neo

config	MACH_PARAMS
	bool
	default y
	select SOC_IMX6
	select SOC_IMX6SX

config PHYS_ADDR
	hex
	default 0x80000000

config PHYS_SIZE
	hex
	default 0x40000000

config	EARLY_DEBUG_ADDR
	hex
	default 0x02000000
